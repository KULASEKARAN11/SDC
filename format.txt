#include <stdio.h>

int main(int argc, char *argv[])
{
    if(argc < 2)
    {
        printf("Usage: %s <input>\n", argv[0]);
        return 0;
    }

    printf("User input: ");
    printf(argv[1]);   // Vulnerable line

    return 0;
}